# Local website (Vite) and API ports — edit these to use different ports.
# Keep VITE_API_PORT in sync with backend `PORT` in backend/.env (default 5000).
VITE_DEV_SERVER_PORT=5173
VITE_API_PORT=5000
